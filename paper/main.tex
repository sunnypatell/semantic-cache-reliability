% SPDX-License-Identifier: LicenseRef-Proprietary
% Copyright (c) 2026 Sunny Patel. All rights reserved.
%% ============================================================================
%% When Semantic Caches Lie
%% Independent research paper by Sunny Patel
%% Diamond-standard preamble (XCharter text + newtxmath, biblatex/biber, booktabs)
%% Compile: latexmk -pdf -cd main.tex   (biber auto-detected from biblatex)
%% ============================================================================
\documentclass[11pt,letterpaper]{article}

%% --- Math first (amsmath/mathtools before newtxmath, per newtx docs) ---------
\usepackage{amsmath}
\usepackage{mathtools}

%% --- Fonts: XCharter text + matched Charter math (single math package) -------
%% newtxmath provides the AMS symbol set, so amssymb is intentionally NOT loaded
%% (loading it clashes with newtxmath over \Bbbk).
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{XCharter}
\usepackage[charter]{newtxmath}

%% --- Microtypography ---------------------------------------------------------
% Protrusion (character protrusion into the margin) is the larger visual win and is
% cheap; font expansion is disabled because generating expanded Type1 instances for the
% many newtx symbol fonts is pathologically slow at this font count.
\usepackage[protrusion=true,expansion=false,final]{microtype}

%% --- Geometry & spacing ------------------------------------------------------
%% A slightly tighter measure (~72 characters) reads better than the article default.
\usepackage[letterpaper,margin=1.1in]{geometry}

%% --- Running head + first-page preprint footer -------------------------------
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\scshape\footnotesize When Semantic Caches Lie}
\fancyhead[R]{\footnotesize\thepage}
\renewcommand{\headrulewidth}{0.3pt}
\fancypagestyle{firstpage}{%
  \fancyhf{}\renewcommand{\headrulewidth}{0pt}%
  \fancyfoot[L]{\footnotesize Preprint.}\fancyfoot[R]{\footnotesize\thepage}}

%% --- Tables ------------------------------------------------------------------
%% Numbers are pre-formatted to a fixed number of decimals by the table-generation
%% code and right-aligned, which aligns the decimal points cleanly. (siunitx's S
%% columns interact pathologically slowly with the newtx math fonts in this toolchain.)
\usepackage{booktabs}
\usepackage{threeparttable}
\usepackage{dcolumn}            % decimal-aligned numeric columns (siunitx is too slow with newtx)
\newcolumntype{d}[1]{D{.}{.}{#1}}

%% --- Algorithms (classic algorithmic + algorithm float; caption honours the en-dash labelsep) ---
\usepackage{algorithm}
\usepackage{algorithmic}

%% --- Graphics & figures ------------------------------------------------------
\usepackage{graphicx}
\usepackage[dvipsnames]{xcolor}
\usepackage[labelsep=endash,font=small]{caption}
\captionsetup[algorithm]{labelsep=endash}% match the algorithm float caption to the floats
\usepackage{subcaption}

%% --- Diagrams (TikZ) and the shared semantic color language --------------------
%% The conceptual figures reuse the Okabe-Ito hues of the data figures so that the
%% colour meaning (equivalent=green, false hit=vermillion, structural=blue) is constant.
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,calc,shapes.geometric,backgrounds,fit}
\definecolor{eqgreen}{HTML}{009E73}
\definecolor{fhred}{HTML}{D55E00}
\definecolor{neutral}{HTML}{0072B2}
\providecommand{\Description}[1]{}% no-op alt-text hook (forward-compatible with tagged PDF)

%% --- Lists & quotes ----------------------------------------------------------
\usepackage{enumitem}
\setlist[itemize]{itemsep=4pt,topsep=4pt}
\setlist[enumerate]{itemsep=4pt,topsep=4pt}
\usepackage[american]{babel}
\usepackage{csquotes}

%% --- Hyperlinks (bare, then \hypersetup) then cleveref ----------------------
\usepackage{hyperref}
\definecolor{linknavy}{rgb}{0.10,0.20,0.45}
\definecolor{citegreen}{rgb}{0.00,0.34,0.20}
\definecolor{urlblue}{rgb}{0.10,0.22,0.55}
\hypersetup{
  colorlinks=true,
  hyperfootnotes=false, % author-note (*) mark on the title stays black, not a colored link
  linkcolor=linknavy,   % internal cross-references (sections, figures, equations)
  citecolor=citegreen,  % citations
  urlcolor=urlblue,     % external links
  breaklinks=true,
  pdflang=en,
  pdftitle={When Semantic Caches Lie: False Hits, Downstream Cost, and Calibration},
  pdfauthor={Sunny Patel},
  pdfsubject={Reliability and cost-aware calibration of embedding-based semantic caching for large language models},
  pdfkeywords={semantic caching, large language models, embedding models, false-hit rate, calibration, retrieval}
}
\usepackage{cleveref}
%% bookmark (loaded after hyperref/cleveref) writes a robust, correctly-nested PDF outline.
\usepackage{bookmark}

%% --- Author identity marks (clickable ORCID iD + icon links, no bare URLs) ---
\usepackage{orcidlink}
\usepackage{fontawesome5}

%% --- Bibliography ------------------------------------------------------------
\usepackage[backend=biber,style=numeric-comp,sorting=nyt,sortcites=true,
            maxbibnames=99,giveninits=true,url=false,doi=true,eprint=true]{biblatex}
\addbibresource{references.bib}

%% --- Shared macros and notation ----------------------------------------------
\input{sections/macros}

%% --- Widow/orphan control ----------------------------------------------------
\widowpenalty=10000
\clubpenalty=10000
\frenchspacing  % single inter-sentence spacing (Bringhurst/Butterick), the modern standard

%% --- Float placement -------------------------------------------------------
%% Relax LaTeX's conservative defaults so figures stay near their references in
%% float-dense sections (rather than drifting pages forward); standard tuned values.
\renewcommand{\topfraction}{0.9}
\renewcommand{\bottomfraction}{0.8}
\renewcommand{\textfraction}{0.1}
\renewcommand{\floatpagefraction}{0.75}
\setcounter{topnumber}{3}
\setcounter{totalnumber}{4}

%% --- Title -------------------------------------------------------------------
\title{\bfseries When Semantic Caches Lie\\[5pt]
{\normalfont\large Why One Similarity Threshold Cannot Make an LLM Cache Both Safe and Useful Across Domains}}
\author{Sunny Patel\,\orcidlink{0009-0005-3863-7642}%
\thanks{\raggedright Copyright \textcopyright{} 2026 Sunny Patel. All rights reserved; the
manuscript and its figures are the author's intellectual property, released for reproduction
only (see the project license). Correspondence:
\href{mailto:sunnypatel124555@gmail.com}{\nolinkurl{sunnypatel124555@gmail.com}}.}\\[3pt]
\small Independent Researcher, Toronto, Canada\\[3pt]
\small \faGlobe~\href{https://www.sunnypatel.net}{sunnypatel.net}\quad\textbar\quad
\faGithub~\href{https://github.com/sunnypatell/semantic-cache-reliability}{sunnypatell/semantic-cache-reliability}}
\date{}

\begin{document}
\maketitle
\thispagestyle{firstpage}

\begin{abstract}
\setlength{\leftskip}{0.045\textwidth}\setlength{\rightskip}{0.045\textwidth}
\noindent
A semantic cache reuses a stored answer when a new prompt's embedding is close enough to an
earlier one, trading a model call for a lookup. The property such a cache actually needs,
whether one prompt's stored answer is valid for another, is not the cosine similarity it can
measure, and the cache fails exactly where the two diverge. Grounding this
\emph{cache-equivalence} in human-labeled prompt pairs, we map the false-hit rate of six
sentence embeddings across three domains with bootstrap confidence intervals. The sharpest
finding is an inversion: on adversarial near-duplicates a classical lexical baseline separates
equivalent from non-equivalent prompts better than every neural encoder, because the
surface-form insensitivity that makes an embedding useful is what blinds it to a word-scrambled
paraphrase; even a joint cross-encoder verifier does not recover the case, and a larger encoder
barely helps. Holding
the false-hit rate under one percent caps coverage near nine percent on news and under one
percent elsewhere. Under a realistic penalty a fixed threshold costs several times more than not
caching. A threshold calibrated to the measured cost recovers near-baseline cost, but transfers
across domains only in the conservative direction. We release the protocol so
that any team can measure this gap on its own encoder and price it into the threshold.
\end{abstract}

\begin{center}\begin{minipage}{0.91\textwidth}\small
\textbf{Keywords:} semantic caching; large language models; embedding models; false-hit rate;
retrieval-augmented generation; threshold calibration; reliability.

\smallskip\noindent
\textbf{Plain-language summary:} An LLM cache reuses an old answer when a new question looks
similar. We show it often serves confident wrong answers when questions look alike but mean
different things, measure the resulting damage, and show how to set the similarity cutoff that
decides when reuse is safe.
\end{minipage}\end{center}
\medskip

\input{sections/introduction}

\input{sections/setup}

\input{sections/results}

\input{sections/downstream}

\input{sections/calibration}

\input{sections/related}

\input{sections/discussion}

\input{sections/conclusion}

\printbibliography

\appendix
\input{sections/appendix}

\end{document}
